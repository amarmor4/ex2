<Window x:Class="ex2.SinglePlayerGame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MazeGUI.Controls"
        mc:Ignorable="d"
        Title="{Binding MazeName}" Height="300" Width="300">
    <Grid TextBlock.FontSize="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="stnRestart" HorizontalAlignment="left" Margin="53,5,0,6">Restart game</Button>
        <Button x:Name="stnSolve" HorizontalAlignment="Center" Margin="0,5,0,6">Solve maze</Button>
        <Button x:Name="stnMainMenu" HorizontalAlignment="right" Margin="0,5,61,6">Main menu</Button>
        <controls:MazeBoard x:Name="mazeBoard" Grid.Row="1"></controls:MazeBoard>
    </Grid>
</Window>
